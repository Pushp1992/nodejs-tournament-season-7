// all routes HTTP method (GET/PUT/POST/DELETE) will be present here

module.exports = (app) => {
    // import your existing Course controller here, which contains method for all CRUD operations

    // APi endpoint to create a new Course Data

    // API endpoint to get all Course Data

    // API endpoint to get single Course Data

    // API endpoint to update Course Data with CourseId

    // API endpoint to delete Course Data with CourseId

};
// All controller logic for CRUD operation will stay here

const CourseModel = require('../model/course-model');

// Create and Save a new course object
exports.create = (req, res) => {
    
    // validate request body

    // Creaate course object

    // Save course object in Database
 
};


// Retrieve and return all course object from the database.
exports.findAll = (req, res) => {
   // TODO from below
};

// Find a single course object with a courseId
exports.findOne = (req, res) => {
    // TODO from below
};

// Update a course object identified by the courseId in the request
exports.update = (req, res) => {
    // TODO from below
};

// Delete a course object with the specified courseId in the request
exports.delete = (req, res) => {
    // TODO from below
};